# The SAME Postgres as the main app — copy DATABASE_URL from backend/.env.
# The landing page only ever writes to meta_ads_responses.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
